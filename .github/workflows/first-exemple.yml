name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "my first pipeline"
        
        - name: list files
          run: ls
        
        - name: read file
          run: cat README.md